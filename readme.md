This may help for services, where you cannot paste your text (KVM in browser in my case).

Preparations: install `pyautogui`:
```shell
pip install pyautogui
```

Now just start `main.py` file and put cursor where you need.